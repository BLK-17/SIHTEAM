{
  "name": "skillbridge-ai",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev": "concurrently \"npm --workspace apps/web run dev\" \"npm --workspace apps/api run dev\"",
    "build": "npm --workspace apps/web run build && npm --workspace apps/api run build",
    "lint": "npm --workspace apps/web run lint && npm --workspace apps/api run lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}