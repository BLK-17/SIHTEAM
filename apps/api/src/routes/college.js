import {Router} from 'express';import {store} from '../services/store.js';const r=Router();r.get('/demo',(req,res)=>res.json(store.college.demo));r.post('/demo/courses',(req,res)=>{if(!req.body?.name)return res.status(400).json({error:'name required'});store.college.demo.courses.push(String(req.body.name));res.status(201).json({courses:store.college.demo.courses})});export {r as collegeRouter};
