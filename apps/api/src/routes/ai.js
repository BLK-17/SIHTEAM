import {Router} from 'express';const r=Router();const keywords=['rest api','database','system design','docker','container','cloud','sql','git','testing','ci/cd'];const verbs=['led','built','designed','implemented','improved','increased','managed','created','developed','optimized','launched','reduced','automated'];r.post('/ats',(req,res)=>{const text=String(req.body?.resume||'');const lower=text.toLowerCase();if(!text.trim())return res.status(400).json({error:'resume required'});const matched=keywords.filter(k=>lower.includes(k));const hasEmail=/\S+@\S+\.\S+/.test(text),hasPhone=/(\+?\d[\d\-\s]{8,}\d)/.test(text);const structure=[hasEmail||hasPhone,/\bskills\b/i.test(text),/\bexperience\b/i.test(text),/\beducation\b/i.test(text)].filter(Boolean).length/4*100;const nums=(text.match(/\d+(\.\d+)?%|\b\d{2,}\b/g)||[]).length;const impact=Math.min(100,nums*18+verbs.filter(v=>lower.includes(v)).length*6);const words=text.trim().split(/\s+/).length;const length=words<80?35:words<150?65:words<=500?100:words<=800?85:60;const formatting=/\t{2,}|\|{2,}/.test(text)?55:92;const keyword=matched.length/keywords.length*100;const overall=Math.round(keyword*.3+structure*.2+impact*.2+length*.15+formatting*.15);const missing=keywords.filter(k=>!lower.includes(k));const issues=[];if(!hasEmail)issues.push('Add an email address.');if(!hasPhone)issues.push('Consider adding a phone number.');if(!/\bskills\b/i.test(text))issues.push('Add a Skills section.');if(!/\bexperience\b/i.test(text))issues.push('Add an Experience section.');if(!/\beducation\b/i.test(text))issues.push('Add an Education section.');if(!nums)issues.push('Add measurable outcomes to achievements.');if(missing.length)issues.push(`Missing Backend Developer keywords: ${missing.slice(0,5).join(', ')}.`);if(!issues.length)issues.push('Strong ATS-ready structure and keyword coverage.');res.json({overall,categories:[{name:'Keyword match',score:Math.round(keyword)},{name:'Structure & sections',score:Math.round(structure)},{name:'Quantified impact',score:Math.round(impact)},{name:'Length',score:length},{name:'Formatting',score:formatting}],issues})});r.post('/chat',(req,res)=>{const m=String(req.body?.message||'').toLowerCase();const ctx=req.body?.context||{};if(m.includes('talent')||m.includes('candidate'))return res.json({answer:`There are currently ${(ctx.talentCount||1240).toLocaleString()} verified candidates in the pipeline.`});if(m.includes('course')||m.includes('curriculum'))return res.json({answer:'Prioritize courses that map directly to current industry demand, especially Cloud & DevOps and Applied GenAI.'});if(m.includes('top')||m.includes('highest')){const d=ctx.demand||[['Cloud & DevOps',88],['Applied GenAI',81]];const top=[...d].sort((a,b)=>b[1]-a[1])[0];return res.json({answer:`The highest current demand signal is ${top[0]} at ${top[1]}% projected growth.`})}return res.json({answer:'For the Backend Developer path, close database migrations and system design gaps next, then verify the work through an assessment or project.'})});export {r as aiRouter};
