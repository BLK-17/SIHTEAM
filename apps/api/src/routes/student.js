import {Router} from 'express';import {store} from '../services/store.js';const r=Router();r.get('/demo',(req,res)=>res.json(store.students.demo));r.post('/demo/skills',(req,res)=>{const {name,have,need}=req.body;if(!name)return res.status(400).json({error:'name required'});store.students.demo.skills.push({name:String(name),have:Number(have)||0,need:Number(need)||80});res.status(201).json(store.students.demo.skills.at(-1))});export {r as studentRouter};
