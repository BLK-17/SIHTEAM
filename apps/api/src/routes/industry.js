import {Router} from 'express';import {store} from '../services/store.js';const r=Router();r.get('/demo',(req,res)=>res.json(store.industry.demo));r.get('/candidates',(req,res)=>{const q=String(req.query.q||'').toLowerCase();const min=Number(req.query.minScore||0);res.json(store.industry.demo.candidates.filter(c=>c.score>=min&&(!q||c.skills.some(s=>s.toLowerCase().includes(q)))))});export {r as industryRouter};
