import express from 'express';import cors from 'cors';import helmet from 'helmet';import morgan from 'morgan';import {studentRouter} from './routes/student.js';import {collegeRouter} from './routes/college.js';import {industryRouter} from './routes/industry.js';import {aiRouter} from './routes/ai.js';
const app=express();app.use(helmet());app.use(cors());app.use(express.json({limit:'2mb'}));app.use(morgan('dev'));app.get('/api/health',(req,res)=>res.json({ok:true,service:'skillbridge-api'}));app.use('/api/students',studentRouter);app.use('/api/college',collegeRouter);app.use('/api/industry',industryRouter);app.use('/api/ai',aiRouter);const port=process.env.PORT||4000;app.listen(port,()=>console.log(`SkillBridge API running on ${port}`));
