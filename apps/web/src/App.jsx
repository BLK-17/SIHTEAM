import {BrowserRouter,Routes,Route,Navigate,useParams} from 'react-router-dom';import Layout from './components/Layout';import Student from './pages/Student';import College from './pages/College';import Industry from './pages/Industry';import './index.css';
function StudentRoute(){const {section}=useParams();return <Student section={section||'dashboard'}/>};function CollegeRoute(){const {section}=useParams();return <College section={section||'curriculum'}/>};function IndustryRoute(){const {section}=useParams();return <Industry section={section||'talent'}/>};
export default function App(){return <BrowserRouter><Routes><Route element={<Layout/>}><Route path="/" element={<Navigate to="/student" replace/>}/><Route path="/student" element={<Student/>}/><Route path="/student/:section" element={<StudentRoute/>}/><Route path="/college" element={<College/>}/><Route path="/college/:section" element={<CollegeRoute/>}/><Route path="/industry" element={<Industry/>}/><Route path="/industry/:section" element={<IndustryRoute/>}/></Route></Routes></BrowserRouter>}
